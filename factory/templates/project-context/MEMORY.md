<!--
  Durable, non-obvious facts learned while building. Gotchas, external
  constraints, "we tried X, it failed because Y", surprising behaviour of a
  dependency or an integration. Maintainer: any agent, via the task PR.

  Newest entries at the bottom — the assembler surfaces the last few bullets.
  One fact per bullet. No secrets, no personal data.
-->

# Project memory

- <!-- fill: e.g. "The vendor sandbox rate-limits to 5 req/s; batch webhook replays." -->
